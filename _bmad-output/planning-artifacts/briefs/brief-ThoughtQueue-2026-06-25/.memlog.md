---
topic: ThoughtQueue — evolve into a filesystem-backed personal markdown notes app (brownfield)
updated: 2026-06-25T21:28
---

- (decision) Stakes: personal passion project, daily driver for the user himself; brief kept lean, no investor-grade rigor. Working mode: Fast path (batch gaps, draft with [ASSUMPTION] tags, iterate).
- (decision) Build approach: GREENFIELD. Archive the current ThoughtQueue codebase, use it as reference only, build fresh with NO database (filesystem is the store). Reuse proven patterns (menu-bar shell, CGEvent capture, Claude keyboard-sim integration) but not the SQLite store.
- (decision) Success signal: the user uses it daily and stops reaching for Apple Notes / scratch files. Personal-utility metric, not growth metrics.
- (decision) Platform: macOS only, single user (the author), no deadline.
- (decision) Local LLM = Apple Foundation Models framework (macOS 26, WWDC2025): free on-device ~3B model, Swift API, guided generation for structured category output. Fits no-account/private/offline principles. Caveat: requires macOS 26 + Apple Intelligence-capable device. Fallback: Ollama/MLX if built-in model insufficient.
- (decision) Top risk = local-LLM speed/latency for auto-title/categorize. Mitigation: run inference async and non-blocking; capture is instant; the review-toast surfaces the result after the fact and is editable. Auto-intelligence is a Later-phase feature, not MVP-blocking.
- (event) Drafted brief.md (lean, personal-project sized) + addendum.md (existing-codebase reference, Apple Foundation Models research, JTBD detail, rejected frontmatter idea, two-way-sync open questions). Awaiting user review (fast path).
- (override) User moved local-LLM auto-title/categorize (+ review toast) from Later into the MVP (overrides intent doc which had it as Later). Consequence: MVP now requires macOS 26 + Apple Intelligence device; latency risk is MVP-critical — capture must run inference fully async and degrade gracefully on unsupported setups.
- (event) Finalized: editorial structure+prose passes applied to brief.md and addendum.md; 19 em dashes removed per author style rule. Brief = lean personal-project brief; addendum = downstream depth. No external handoffs configured.
